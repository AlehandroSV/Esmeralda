import { describe, it, expect } from "vitest";

describe("esmerald init", () => {
  it.todo("creates project directory structure");
  it.todo("creates jade.config.lua");
  it.todo("creates schema/init.lua");
  it.todo("creates lib/app.lua");
});
